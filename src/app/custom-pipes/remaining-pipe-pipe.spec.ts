import { RemainingPipe } from './remaining-pipe';

describe('RemainingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingPipe();
    expect(pipe).toBeTruthy();
  });
});
